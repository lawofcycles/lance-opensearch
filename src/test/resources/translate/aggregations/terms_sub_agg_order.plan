LanceAggregate(group=[{0}], m=[AVG($1)], buckets=[[TERMS{name=by_cat, size=10, shardSize=25, minDocCount=1, order=SUB_AGGREGATION(m, desc)}]], metrics=[[AVG{name=m}]])
  LogicalProject(by_cat=[$4], price=[$2])
    LanceTableScan(table=[[lance, idx]])
