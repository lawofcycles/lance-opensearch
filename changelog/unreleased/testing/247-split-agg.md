- `AggregatePushdownShapes`, the executor's shape predicates over a pushed aggregate (`isPushdownMetric`, `isPushdownComposite`, `isPushdownCompositeSource`, `aggregationOrder`, `calendarUnit`), has unit tests of its own in `AggregatePushdownShapesTests`. They pin the builder shapes each predicate accepts and refuses (tdigest against hdr percentiles, scripts, `missing` and `value_type` hints, composite date sources with a fixed length interval against month and week units, offsets and time zones, the `_key` tie breaker the terms builder adds around a sub aggregation order, and the `date_trunc` unit of every calendar interval). No production code changes. (#247)
