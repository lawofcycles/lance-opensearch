- The repository drops the governance files it had taken from the opensearch-project plugin repository template before it belongs to that organization: `MAINTAINERS.md`, `ADMINS.md`, `RELEASING.md`, `CODE_OF_CONDUCT.md`, `SECURITY.md`, the `untriaged` label workflow (`add-untriaged.yml`), the `release-notes/` directory and the issue template contact links to them. `README.md` is slimmed to an overview, the preview status, a quick start, the documentation index, a short contributing paragraph and the license; the feature list, requirements and install steps it repeated live in `docs/features.md` and `docs/getting-started.md`. `CONTRIBUTING.md` keeps the Developer Certificate of Origin and the changelog fragment rules, loses the backport section, and asks for security issues to be reported as GitHub issues.
