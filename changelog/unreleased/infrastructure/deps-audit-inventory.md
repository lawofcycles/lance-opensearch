- The dependency closure, the `thirdPartyAudit` ignore lists, the license files and the security policy are inventoried for the move to an opensearch-project repository. `docs/dependencies.md` (new) lists every bundled jar with the code that needs it and the artifacts that were tried and left out; `build.gradle` is reordered into sections (metadata, dependencies by group, tests, precommit, publishing) with the audit's 124 `ignoreMissingClasses` and 28 `ignoreViolations` entries regrouped by the jar that holds the reference, one comment per group, every entry re-verified against the current versions; the unreferenced `http-auth-aws-eventstream` carrier jar is dropped. `plugin-security.policy` carries one justification per grant and loses the grants nothing needs: the `java.io.tmpdir` and `<<ALL FILES>>` file grants (the core policy already grants every codebase `path.data` and `java.io.tmpdir`, and the Lance native library's file access is outside the JVM's checks; the plugin's own read of `/proc/meminfo` gets an explicit grant) and `modifyThread` / `modifyThreadGroup` (granted to every codebase by the core policy). `NOTICE.txt` names the bundled projects. No resolved version changes.
