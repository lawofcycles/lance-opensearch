- `CHANGELOG.md`'s `## [Unreleased]` block drops the pre-migration bullets that described the shard path search fallback (`PlanToShardPathRule`, `ShardPathFallbackExec`, "routes through the shard path" mentions on the geo, thread pool, aggregation allow list and max_docs bullets, and the "Scripts, pipelines and the other types still take the shard path" tail); the fallback was retired in D-6e (#256). The two `WIRE_VERSION` mentions in the F-2 and wire format fragments name `1` for `FragmentPlan` and `2` for `LanceExplainResponse` after D-6e's bump. (#259)
