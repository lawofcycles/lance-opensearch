- `DEVELOPER_GUIDE.md` names all ten precommit tasks (`forbiddenApis`, `licenseHeaders`, `jarHell`, `thirdPartyAudit`, `dependencyLicenses`, `testingConventions`, `filepermissions`, `forbiddenPatterns`, `validatePluginZipPom`, `validatePom`) with one line each on what the check rejects, and explains that `validatePom` aggregates the per publication POM checks (`validatePluginZipPom`, `validateNebulaPom`) so a reader no longer finds a different task list in the guide and in the release notes. `docs/design/namespace-freshness.md` gains a paragraph on the runtime cost of the freshness service: one generic pool task per tracked index at `lance.namespace.poll_cadence`, one manifest open per task through the node-scoped `LanceRegistry.SESSION`, cost linear in the number of Lance backed shards on the node, and the freshness check and the search coordinator opening the same table URI from different pools while sharing the `Session` and the Arrow allocator but not the `Dataset` handles.
