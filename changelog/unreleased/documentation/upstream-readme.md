- `README.md` is rewritten for readers who meet the plugin in the opensearch-project organization: a one line description, an overview of what attaching a Lance table gives, a status section that states the preview standing (the read side works end to end, interfaces can change between releases, every node must run the same plugin version because the plugin's inter node messages carry a version marker and rolling upgrades are not supported yet), a feature summary that mirrors `docs/features.md`, the requirements (OpenSearch 3.8.0, JDK 21, the three platforms the Lance native library ships for), the `opensearch-plugin install` command with the two JVM options every node needs, a documentation index over every page under `docs/`, the release cadence from `RELEASING.md`, and the contribution path (`CONTRIBUTING.md`, the Developer Guide, governance, DCO). `docs/getting-started.md` names the freshness check on the shard's node where it described the manifest follow forward as the namespace poll, and its cleanup step describes the cluster state namespace registration that survives a restart instead of an in process registry that no longer exists. `CHANGELOG.md`'s `## [Unreleased]` block drops the entries that described an earlier README and an explain envelope restriction that no longer applies, and no longer claims an automatic index build, which the plugin does not perform.
