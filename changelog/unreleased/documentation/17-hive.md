- Record why the Hive 2 and Hive 3 metastore catalogs (`lance-namespace-hive2` / `lance-namespace-hive3` 0.4.1) are not bundled: neither needs `hive-exec`, but the two Hive major versions collide on 6782 class names (`hive-metastore` vs `hive-standalone-metastore`, `hive-common` 2 vs 3, Hadoop 2.8.5 vs 3.1.0) so at most one could ship, each alone adds 18 to 22 MB with 532 to 557 unresolved references `thirdPartyAudit` would have to enumerate plus Hadoop's `Unsafe` violations, and the thrift client blocks for minutes on an unreachable metastore. `docs/limitations.md` lists the closure, the collisions and what would have to change to ship one. (#17)
