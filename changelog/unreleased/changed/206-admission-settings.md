- The admission gate's settings, label and stats lose their full text prefix now that the gate covers every native path. `lance.admission.enabled`, `lance.admission.headroom` and `lance.admission.bounded_shapes_gated` replace `lance.fts.admission.enabled`, `lance.fts.admission.headroom` and `lance.fts.admission.bounded_shapes_gated`, and `lance.test.admission_available_memory` replaces `lance.test.fts_admission_available_memory`, with no alias (the plugin has no release). The 429 is labelled `lance_admission` instead of `lance_fts_admission` and its message reads `[lance_admission] <kind> estimate [X] exceeds available [Y] minus headroom [Z] plus [R] retained by earlier admitted scans: ...`. In `GET /_lance/stats` the `fts.admission` block moves to a top level `admission` block with `enabled`, `headroom_bytes`, `available_bytes`, `retained_bytes`, `last_estimate_bytes`, `last_kind` and `rejections`, the last an object with one counter per kind (`fts`, `scalar_index`, `vector_index`, `filter_scan`, `aggregate_scan`, `column_load`), every counter present even at zero; `fts` keeps `subset_probe_limit`. (#206)
