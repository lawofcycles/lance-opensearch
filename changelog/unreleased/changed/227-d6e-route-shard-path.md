- `GET /{index}/_lance/explain` no longer answers `route: shard_path` or a `reasons` list. A body carrying `suggest` or `highlight` answers 200 with `route: unsupported` and the refusal message under `unplanned`, the message a `_search` with the same body is refused with as 400; `logical`, `physical`, `fragment_plan` and `refinements_possible` are absent on that route since nothing is planned. Every other body keeps `route: fragment`. (#227)
