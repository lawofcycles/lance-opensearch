- `GET /<index>/_lance/explain` no longer reports `aggregation tree outside the pushdown shapes, or lance.aggregation.pushdown is false` as `unplanned`. An aggregation the translator does not spell is now named by the translator's own message (`aggregation type [top_hits]`, `two top level aggregations`, `bucket tree deeper than 3 levels`, ...), and `lance.aggregation.pushdown: false` is a cost decision rather than a refusal, so the response carries no `unplanned` element for it and the physical plan shows `LuceneAggregateExec`. (#155)
